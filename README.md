# promise

A flakey server for learning JS promises.

## setup

To run:

```bash
python -m venv venv
. venv/bin/activate
pip install -r requirements.txt 
python server.py
```

Then navigate to [](http://localhost:8080)